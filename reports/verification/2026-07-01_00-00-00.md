# Agent Verification Report

**Project:** LinkScout — LinkedIn scraper & job matching platform
**Date:** 2026-07-01
**Mode:** Standalone
**Language:** TypeScript (Next.js) + Rust (Axum worker)
**Agent framework:** None detected (web application with AI/LLM integration via Groq API)
**Files analyzed:** 54 (29 TypeScript/TSX + 25 Rust)
**Kahuna:** Not detected

## Summary

✅ 22 checks passed | ⚠️ 11 warnings | ❌ 1 issue

### By Category
| Category | Pass | Warn | Issue |
|----------|------|------|-------|
| Security | 5 | 2 | 0 |
| Patterns | 5 | 1 | 1 |
| Quality | 3 | 5 | 0 |
| Language | 4 | 3 | 0 |

---

## Security

### Secrets
- [x] No hardcoded secrets or API keys
- [x] All credentials loaded from environment variables (`env::var`, `process.env`)
- [x] No API key patterns (`sk-...`, `AKIA...`, `ghp_...`, etc.) in source code

### Dependencies
- [x] TypeScript (`package.json`): all deps use caret ranges (`^x.y.z`) — ✅ acceptable
- [ ] ⚠️ Rust (`Cargo.toml`): deps use loose semver (`"1"`, `"0.8"`, `"0.12"`, `"0.22"`) — not pinned to exact versions

### Input Validation
- [x] Zod schemas used for API input validation (`intent/route.ts`)
- [x] Rust routes validate non-empty keyword (`scrape.rs:37`)
- [x] Serde deserialization provides basic type validation
- [ ] ⚠️ Custom URL encoding function in `db/supabase.rs:244` — basic but not RFC 3986 compliant

### Error Handling
- [x] Sanitized error messages returned to users (no stack traces)
- [ ] ⚠️ `intent/route.ts:128` returns raw error message in production responses — could leak internal details

### Secure Defaults
- [x] CORS uses predicate-based origin allow-list (`main.rs:107`)
- [x] Security headers set (`X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`)
- [x] Trailing slash handling and CSP headers via middleware
- [x] Auth middleware validates tokens via Supabase Auth API

---

## Agent Patterns

### Loop Safety
- [x] `retry.rs:28` — `loop` has explicit termination via `attempt >= max_attempts`
- [x] No infinite `while true` or unbounded loops in codebase

### Retry Limits
- [x] `groq.rs:65` — retry with `GROQ_MAX_ATTEMPTS: 3`
- [x] `apify.rs:61` — retry with `APIFY_MAX_ATTEMPTS: 3`
- [x] `retry.rs:19` — generic `with_retry()` takes explicit `max_attempts` parameter
- [ ] ❌ `intent/route.ts` — Groq API call has no retry logic; single attempt then fallback. If the API returns a transient 5xx, the user gets a degraded fallback response instead of a retry.

### Tool Consistency
- [x] No AI agent tool registry to verify (web application, not agent framework)

### Context Size
- [x] System prompts well within limits (~250 tokens for analysis, ~50 for pitch)
- [x] No oversized prompt files

---

## Quality

### Naming Conventions
- [x] Rust: consistent `snake_case` for functions/variables, `CamelCase` for types
- [x] TypeScript: consistent `camelCase` for variables/functions
- [ ] ⚠️ `DashboardClient.tsx:103` — `FILTERS` const uses mixed French/English labels

### Code Organization
- [x] Clean modular structure: Rust modules (`ai/`, `db/`, `routes/`, `scraper/`, etc.)
- [x] TypeScript well-organized: `components/`, `lib/`, `app/`, `context/`
- [ ] ⚠️ `DashboardClient.tsx` — 633 lines (exceeds 500-line recommendation)
- [ ] ⚠️ `guest.rs` — 200 lines for a single scraper implementation

### Magic Numbers
- [x] Retry attempt counts, timeouts, and limits defined as named constants
- [ ] ⚠️ `guest.rs:46` — magic literal `25` (batch limit) should be a named constant

### Documentation
- [x] Rust: doc comments on key public functions (`retry.rs`, `db/supabase.rs`)
- [x] TypeScript: JSDoc comments on major API client functions
- [ ] ⚠️ `cache.rs`, `quota.rs` — public functions lack doc comments
- [ ] ⚠️ Several Rust structs lack doc comments vs models which are well-documented

### Error Handling
- [x] Rust: proper `Result`/`Option` patterns with tracing
- [x] TypeScript: structured try/catch with error logging
- [ ] ⚠️ `db/supabase.rs:26` — `.unwrap()` on HeaderValue::from_str (could panic)
- [ ] ⚠️ `quota.rs:44`, `cache.rs:42` — `Mutex::lock().unwrap()` (poison panic risk)

### Code Duplication
- [ ] ⚠️ `quota.rs:42-79` and `quota.rs:82-107` — `check_and_increment` and `peek` share ~80% logic

---

## Language (TypeScript + Rust)

### TypeScript
- [x] Strict TypeScript with thorough type annotations across the codebase
- [x] Proper async/await error handling patterns
- [x] Zod schemas for runtime validation
- [ ] ⚠️ `server-jobs.ts:13` — `SupabaseClient<any, any, any, any, any>` — excessive `any` types
- [ ] ⚠️ `api-client.ts:126-130` — `AnalyzeJobInput` type has `unknown` fields that should be typed

### Rust
- [x] Idiomatic use of `?` operator, `async/await`, trait objects
- [x] Clean separation of concerns via modules
- [ ] ⚠️ Several `.unwrap()` calls on `Result` types — could panic at runtime
- [ ] ⚠️ Cargo.toml edition `2024` — modern but early-adoption risk

---

## Detailed Findings

> `[P]` = pattern-matched (structurally reliable) · `[H]` = heuristic (best-effort judgment)

### ✅ Passing
- `[P]` No hardcoded secrets or API keys in source code
- `[P]` All retry decorators/functions have explicit limit parameters
- `[P]` No infinite or unbounded loops detected
- `[P]` Security headers set in middleware
- `[P]` Auth middleware validates tokens via Supabase
- `[P]` Input validated with Zod schemas (TypeScript) and Serde (Rust)
- `[H]` Naming conventions consistent within each language
- `[H]` Clean module organization across both Rust and TypeScript
- `[H]` Error messages sanitized in API responses (no stack trace leakage)
- `[H]` CORS properly configured with allow-list pattern

### ⚠️ Warnings
- `[P]` **Unpinned Cargo dependencies**: `Cargo.toml` uses loose semver (`"1"`, `"0.8"`)
  - **Location:** `worker-rs/Cargo.toml:6-26`
  - **Category:** Security
  - **Suggestion:** Pin to exact versions or use `cargo deny` to audit

- `[H]` **Custom URL encoding**: `db/supabase.rs:244` has a basic URL encoder
  - **Location:** `worker-rs/src/db/supabase.rs:244-256`
  - **Category:** Security
  - **Suggestion:** Use `urlencoding` crate or `serde_urlencoded` instead of custom impl

- `[H]` **Error details exposed**: `intent/route.ts:128` returns raw error message
  - **Location:** `linkscout/app/api/intent/route.ts:128`
  - **Category:** Security
  - **Suggestion:** Return a generic error message; log the details server-side

- `[P]` **Missing retry on Groq API call**: `intent/route.ts` has no retry mechanism
  - **Location:** `linkscout/app/api/intent/route.ts:65`
  - **Category:** Patterns
  - **Suggestion:** Wrap the fetch call with a retry (2-3 attempts with backoff)

- `[H]` **Large component**: `DashboardClient.tsx` is 633 lines
  - **Location:** `linkscout/app/[locale]/dashboard/DashboardClient.tsx`
  - **Category:** Quality
  - **Suggestion:** Extract sub-components; split into focused hooks

- `[H]` **Magic number**: `guest.rs:46` literal `25` for batch limit
  - **Location:** `worker-rs/src/scraper/guest.rs:46`
  - **Category:** Quality
  - **Suggestion:** Define `const BATCH_LIMIT: u32 = 25;`

- `[H]` **Code duplication**: `quota.rs:check_and_increment` and `peek` share logic
  - **Location:** `worker-rs/src/quota.rs:42-107`
  - **Category:** Quality
  - **Suggestion:** Extract shared window-reset logic into a helper method

- `[H]` **Panic-prone unwraps**: Multiple `.unwrap()` calls on Mutex/Result
  - **Location:** `worker-rs/src/cache.rs:42`, `worker-rs/src/quota.rs:44`, `worker-rs/src/db/supabase.rs:26`
  - **Category:** Language (Rust)
  - **Suggestion:** Use `?` or `.expect("context")` for clarity, or handle poisoning properly

- `[H]` **Excessive `any` types**: `SupabaseClient<any, any, any, any, any>`
  - **Location:** `linkscout/lib/server-jobs.ts:13`
  - **Category:** Language (TypeScript)
  - **Suggestion:** Use generated Supabase types instead of `any`

### ❌ Issues
- `[P]` **Missing retry limit on Groq intent API**: The `/api/intent` route calls Groq API with `AbortController` timeout but no retry logic. Transient failures (rate limit, network glitch) degrade the UX to a fallback instead of retrying.
  - **Location:** `linkscout/app/api/intent/route.ts:65`
  - **Category:** Patterns
  - **Rule:** Pattern has retry but no explicit retry limit — single attempt only
  - **Fix:** Wrap the fetch with 2 retries using exponential backoff, similar to the Rust `with_retry` pattern

---

## Recommendations

1. **[High] Add retry logic to `/api/intent` route** — Wraps the Groq API call with 2-3 retries and exponential backoff, matching the robust retry pattern already used in the Rust worker (`retry.rs`).

2. **[Medium] Pin Cargo dependencies** — Add exact version pins or use `cargo deny` / `cargo audit` for reproducible builds.

3. **[Medium] Replace `.unwrap()` calls with proper error handling** — Use `?` operator or `.expect("context")` with meaningful messages to avoid panics in production.

4. **[Low] Extract shared quota logic** — Refactor `check_and_increment` and `peek` to share the window-reset logic.

5. **[Low] Add doc comments to `cache.rs` and `quota.rs`** — Public functions like `Cache::new()`, `quota.peek()` should document their behavior.

6. **[Low] Break up `DashboardClient.tsx`** — Split into smaller components or extract custom hooks for the view rendering logic.

---

*Report generated by Agent Verifier v1.0.0*
